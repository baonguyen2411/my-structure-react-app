import React, { memo } from 'react';

import './styles.scss';

const Setting = () => (
  <div>
    <div>
      <div>
        <h3>Setting Page</h3>
      </div>
    </div>
  </div>
);

Setting.propTypes = {};

export default memo(Setting);
