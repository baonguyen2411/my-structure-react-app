import React, { memo } from 'react';

import './styles.scss';

const LandingPage = () => (
  <div>
    <div>
      <div>
        <h3>Landing Page</h3>
      </div>
    </div>
  </div>
);

LandingPage.propTypes = {};

export default memo(LandingPage);
